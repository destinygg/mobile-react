MobileChat seems pretty good?  need to subclass Message classes and hook them up to react component with
the new formatting functionality.  also need to figure out how UserBadge fits in to it.

plug buildFeatures into react components
